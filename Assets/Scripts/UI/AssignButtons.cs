using UnityEngine;
using UnityEngine.UI;
public class AssignButtons : MonoBehaviour
{
 /*   public ChangeScene GameManager; 
    private void Awake()
    {
        

        if (GameManager != null)
        {
            Button btn = GetComponent<Button>();
            btn.onClick.AddListener(GameManager.TogglePause);
        }
    }

    */
}
